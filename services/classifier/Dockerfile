FROM python:3.12-slim

WORKDIR /app

COPY services/classifier/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app/

ENV PYTHONPATH=/app

EXPOSE 8001

CMD ["uvicorn", "services.classifier.main:app", "--host", "0.0.0.0", "--port", "8001"]
